# RecoverAI Enterprise – Unified Dependency Manifest
# =====================================================
# Streamlit Cloud uses Python 3.14 which has no pre-built wheels for hard-pinned
# packages; lower-bound ranges let pip/uv resolve compatible wheels automatically.
#
# Split into two logical groups:
#   [dashboard]  — Streamlit Cloud needs only this group
#   [api]        — FastAPI server needs api + ml groups
#   [ml]         — shared ML pipeline
#   [dev]        — local dev + CI tooling (not installed in production images)

# ── Core: Settings & validation ───────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.6.0

# ── Dashboard (Streamlit Cloud) ───────────────────────────────────────────────
streamlit>=1.40.0
streamlit-autorefresh>=1.0.1
plotly>=5.20.0
pandas>=2.0.0
Pillow>=10.0.0

# ── FastAPI stack ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0
requests>=2.31.0
openai>=1.50.0
langgraph>=0.2.0
shap>=0.45.0
matplotlib>=3.8.0
reportlab>=4.0.0
psycopg[binary]>=3.2.0

# ── Observability ─────────────────────────────────────────────────────────────
# prometheus-fastapi-instrumentator: Prometheus metrics for FastAPI
# prometheus-client: pulled as transitive dep; pinned floor for Python 3.14 compat
prometheus-fastapi-instrumentator>=6.1.0
prometheus-client>=0.20.0

# ── ML pipeline ───────────────────────────────────────────────────────────────
scikit-learn>=1.3.0
lightgbm>=4.0.0
numpy>=1.26.0
# scipy: used by KS drift test; optional — scorer falls back to pure-Python impl
scipy>=1.11.0

# ── Resilience ────────────────────────────────────────────────────────────────
tenacity>=8.0.0

# ── Security ──────────────────────────────────────────────────────────────────
# cryptography: future Vault / JWT integration; safe to keep floor-pinned
cryptography>=42.0.0

# ── Dev / CI tooling (install with: pip install -r requirements.txt -r requirements-dev.txt) ──
# Kept separate so production images stay lean.
# See requirements-dev.txt

# Optional distributed task queue (enable with USE_CELERY=1)
celery>=5.4.0
redis>=5.0.0
